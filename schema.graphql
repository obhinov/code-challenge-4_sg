type Query {
	getUserById(userId: ID!): User
}

type User {
	id: ID!
	name: String!
	job: String!
}

type Mutation {
    createUser(id: String, name: String, job: String): Boolean
    updateUser(id: String, name: String, job: String): Boolean
    deleteUser(id: String): Boolean
}